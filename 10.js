//using some more function for fun

var gl = 11;

function one() {
    var gl = 10;
    console.log(gl);
}

function two() {
    if (gl > 0) {
        console.log(gl);
    }
}
one();
two();